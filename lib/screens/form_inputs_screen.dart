import 'package:flutter/material.dart';

class FormInputsScreen extends StatelessWidget {
  const FormInputsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Form Inputs"),
      ),
      body: const Center(),
    );
    ;
  }
}
